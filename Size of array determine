#include<bits/stdc++.h>
using namespace std;
int main()
{   
   int arr[] = {12, 2, 5, 8, 6, 10};
   int n = sizeof(arr)/sizeof(arr[0]);
   cout << "Size of the array is " << n << endl;
   for(int i=0; i<n; i++){
      cout << arr[i] << " ";
   }
   cout << endl;
   return 0;
}
